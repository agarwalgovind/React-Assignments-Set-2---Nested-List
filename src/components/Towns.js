import React from "react";

const Towns = ({name}) => {
    return (
        <div>
            <div>
                <button>{name}</button>
            </div>
        </div>
    )

}

export default Towns;